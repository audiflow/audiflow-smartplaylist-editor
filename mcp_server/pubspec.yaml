name: sp_mcp_server
description: MCP server for SmartPlaylist web editor
publish_to: none
resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  http: ^1.6.0
dev_dependencies:
  test: ^1.29.0
