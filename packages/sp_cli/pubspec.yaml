name: sp_cli
description: CLI tools for validating and managing smart playlist configs
publish_to: none
resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  sp_shared:
    path: ../sp_shared

dev_dependencies:
  test: ^1.25.0
